// Placeholder — orbital artwork removed during the cream/gold redesign.
// The hero now relies on typography and whitespace rather than animated SVG.
export default function HeroOrbital() { return null; }
