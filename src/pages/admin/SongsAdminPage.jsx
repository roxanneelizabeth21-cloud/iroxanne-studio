import SongProfiles from '@/pages/marketing/SongProfiles';

// Dedicated admin page for the song knowledge base — promotes Songs out of
// the cluttered marketing pill nav into its own admin sidebar entry.
export default function SongsAdminPage() {
  return <SongProfiles />;
}